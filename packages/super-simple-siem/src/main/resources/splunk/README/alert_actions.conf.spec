[super_simple_siem_alert]

param.time = <string>
* default field name for time - typically will not need to change this

param.max_count = <int>
* default max count of events to create for a single alert

